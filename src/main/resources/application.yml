spring:
  datasource:
    url: jdbc:postgresql://dpg-cneqkp6g1b2c73cvhehg-a.frankfurt-postgres.render.com:5432/cloud_db_cn0h
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
